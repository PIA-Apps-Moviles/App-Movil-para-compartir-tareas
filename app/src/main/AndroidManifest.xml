<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PIA_AppsMov"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".usescases.compartirTarea.Agregar_Tarea"
            android:exported="true" />
        <activity
            android:name=".usescases.user.MiCuenta"
            android:exported="true" />
        <activity
            android:name=".usescases.menu.Menu"
            android:exported="true" />
        <activity
            android:name=".usescases.iniciosesion.InicioSesion"
            android:exported="true" />
        <activity
            android:name=".usescases.registro.Registrarse"
            android:exported="true" />
        <activity
            android:name=".usescases.pantallaprincipal.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>